apiVersion: v1
kind: Service
metadata:
  name: mcp
  namespace: todo-app
  labels:
    app: mcp
spec:
  type: ClusterIP
  selector:
    app: mcp
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  sessionAffinity: None
