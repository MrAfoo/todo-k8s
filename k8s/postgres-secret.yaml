apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: todo-app
  labels:
    app: postgres
type: Opaque
stringData:
  # Change these in production - use external secret management
  POSTGRES_PASSWORD: "changeme_postgres_password"
