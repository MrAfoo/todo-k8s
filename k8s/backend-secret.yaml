apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: todo-app
  labels:
    app: backend
type: Opaque
stringData:
  # Change these in production - use external secret management like Sealed Secrets or External Secrets Operator
  BETTER_AUTH_SECRET: "your-secret-key-min-32-chars-long-change-in-production"
  SECRET_KEY: "your-secret-key-here-change-in-production"
  GROQ_API_KEY: "your-groq-api-key-here"
